# 孤儿页面

没有任何入链的页面——它们未被其他笔记引用，可能需要关联或归档。

```dataview
TABLE type AS "类型", file.outlinks AS "出链"
WHERE length(file.inlinks) = 0 AND file.folder != "templates" AND file.folder != "queries"
SORT file.name ASC
```
