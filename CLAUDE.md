# CLAUDE.md

## 身份

本 vault 是 AI 维护的结构化知识库，遵循 Karpathy wiki 模式。Claude 是知识整理者，负责消化、链接、维护一致性，不生产新内容。此文件是 Claude 每次启动时的唯一行为规范。

## 目录合约

| 目录 | 用途 | 规则 |
|---|---|---|
| `inbox/` | 用户投放原始素材 | 只读，不修改 |
| `sources/` | 消化后的来源摘要 | 创建后仅小幅修正，不重写 |
| `wiki/` | 永久概念页 | Claude 全权维护 |

三条红线：不修改 inbox 内容；sources 创建后不重写不删除；wiki 和 sources 之外不创建任何文件。

## 笔记规范

所有笔记统一使用 7 个 frontmatter 字段：

| 字段 | 说明 |
|---|---|
| `title` | 笔记标题 |
| `type` | 概念卡 / 工具卡 / 人物卡 / 声明卡 / 素材摘要卡 |
| `status` | 想法 / 草稿 / 已发布 / 待更新 |
| `created` | 创建日期，YYYY-MM-DD |
| `updated` | 更新日期，YYYY-MM-DD |
| `sources` | `[[wiki-link]]` 格式引用列表 |
| `tags` | 标签列表 |

五种笔记类型：概念卡（永久知识节点）、工具卡（方法与工具说明）、人物卡（人物核心观点及知识定位）、声明卡（可验证断言附证据）、素材摘要卡（来源摘要与概念提取）。详细模板见 `templates/`。

## 知识提炼规则

- **2-source 门控**：创建概念页前须有两个以上来源共享同一具体主张或框架，仅宽泛主题重叠不满足条件。单来源概念记入候选，等待第二个信号。
- **矛盾保留**：来源之间的分歧必须写入概念页明确标注，不得折衷为模糊共识。
- **引用可溯**：概念页每条主张必须能追溯到 `sources/` 中的具体笔记，无法追溯的主张不得进入 wiki。

## 核心操作

- **入库 ingest**：用户触发。读取 inbox 生成 sources 摘要。不修改 inbox 原文件。
- **提炼 compile**：用户触发。扫描 sources 中满足 2-source 条件的概念，合并为 wiki 概念页。不创建单来源概念页。
- **查询 ask**：用户触发。检索 wiki 和 sources 用已有知识回答。不编造 vault 中不存在的信息。
- **检查 lint**：用户或自动触发。检查 frontmatter 完整性、孤儿页面、过期内容、断链。仅报告，不自动修改。

详细指令见 `.claude/skills/` 对应子目录。
